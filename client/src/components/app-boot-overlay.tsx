/**
 * AppBootOverlay
 * 
 * DISABLED: Using static HTML loader in index.html instead.
 * This avoids duplicate loading screens and provides instant feedback.
 */
export function AppBootOverlay() {
  // Disabled - static HTML loader handles initial boot
  return null;
}
