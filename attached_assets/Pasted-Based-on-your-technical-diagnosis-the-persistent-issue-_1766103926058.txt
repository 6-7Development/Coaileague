Based on your technical diagnosis, the persistent issue with the Universal Notification System (UNS) is a classic State Mismatch between the frontend and backend. You've confirmed the database correctly shows 0 unviewed updates, but the frontend is still displaying a badge with a non-zero count.This suggests that the frontend is either caching a stale count or using a different calculation logic than the backend's markAllPlatformUpdatesAsViewed function.🔍 Root Cause: The Frontend/Backend Logic DriftComponentCurrent State / IssueBackend (DB)Verified 0 unviewed updates. Queries are executing correctly but returning no results.Backend (API)The mark-as-viewed function successfully marks items but doesn't account for frontend-specific visibility filters.Frontend (UI)Displaying an unread badge. Likely using a cached state or a manual local count that hasn't been re-synced with the server.🛠️ Urgent Fix: Re-Syncing the UNSTo resolve this persistent mismatch and ensure Trinity can manage notifications reliably, you must enforce a Single Source of Truth for the count.1. Enforce a "Total Reset" on Mark-as-ReadInstead of just marking items, the markAllPlatformUpdatesAsViewed API should return the new actual count (which should be 0) in the response.Frontend Action: Immediately update the local state with the count returned from the server response, rather than decrementing it manually.2. Synchronize Visibility LogicIf your getUnreadPlatformUpdatesCount uses visibility filters (e.g., hiding updates for certain user roles), the markAsViewed function must use the exact same SQL predicates.Action: Create a shared backend utility function getUnreadPlatformUpdatesQuery() that both the Count and Mark functions use to ensure they are always looking at the same set of records.3. Clear Local Storage / CacheIf you are using a state management library (like Redux or Zustand) or browser localStorage, a stale value might be persisting across refreshes.Action: Force a re-fetch of the notification count whenever the user opens the notification popover or when a "mark all as read" action completes.🚀 Next Step for TrinityWould you like me to generate a "Registry Audit" script that Trinity can run to compare every notification in the database against the frontend's badge count to find the exact ID causing the drift?